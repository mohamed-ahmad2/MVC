@model List<ProjectMVC1.Models.Instructore>

@{
    ViewBag.Title = "Instructors List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="mt-4">Instructors</h1>

<table class="table table-striped table-bordered table-hover">
    <thead class="table-dark">
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Image</th>
            <th>Address</th>
            <th>Salary</th>
            <th>Department</th>
            <th>Course</th>
            <th colspan="2" class="text-center">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var instructor in Model)
        {
            <tr>
                <td>@instructor.InstructoreId</td>
                <td>@instructor.Name</td>
                <td>
                    <img src="~/images/@instructor.Image" alt="@instructor.Name" class="img-thumbnail" style="width:100px; height:100px;" />
                </td>
                <td>@instructor.Address</td>
                <td>@instructor.Salary</td>
                <td>@instructor.Department?.Name</td>
                <td>@instructor.Course?.Name</td>
                <td class="text-center">
                    <a asp-controller="Instructore" asp-action="Details" asp-route-id="@instructor.InstructoreId" class="btn btn-info btn-sm">
                        <i class="bi bi-eye"></i> View
                    </a>
                </td>
                <td class="text-center">
                    <a asp-controller="Instructore" asp-action="Edit" asp-route-id="@instructor.InstructoreId" class="btn btn-warning btn-sm">
                        <i class="bi bi-pencil"></i> Edit
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
<a asp-action="New" asp-controller="Instructore" class="btn btn-success btn-sm">New Instructore</a>
